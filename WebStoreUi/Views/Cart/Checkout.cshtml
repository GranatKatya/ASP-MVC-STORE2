

@model WebStoreDomain.Entities.ShippingDetails

@{
    ViewBag.Title = "Dushka: форма заказа";
}

<h2>Оформить заказ сейчас</h2>
<p>Пожалуйста введи ваши контактные данные, и мы сразу отправим товар!</p>


@using (Html.BeginForm())
{
    @Html.ValidationSummary();
    <h3>Data</h3>
    <div class="form-group">
        <label>Name:</label>
        @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
    </div>


    foreach (var property in ViewData.ModelMetadata.Properties)
    {
        if (property.PropertyName != "Name" && property.PropertyName != "GiftWrap" 
            && property.PropertyName !="SelectedItemId"    && property.PropertyName !="Items" 
            && property.PropertyName !="DeliveryMethod"    && property.PropertyName !="PaymentMethod"
            && property.PropertyName != "DeliveryMethodId" && property.PropertyName != "PaymentMethodId"
              && property.PropertyName != "SelectedOrderId" && property.PropertyName != "" )
        {
            <div class="form-group">
                <label>@(property.DisplayName ?? property.PropertyName)</label>
                @Html.TextBox(property.PropertyName, null, new { @class = "form-control" })
            </div>
        }
    }

    @*<h3>Delivery method</h3>*@
    @*<div>
        @Html.LabelFor(model => model.DeliveryMethod)
        @Html.DropDownListFor(model => model.DeliveryMethod, ViewBag.DeliveryMethods as SelectList)
    </div>
    <h3>Payment method</h3>
    <div>*@
       @* @Html.LabelFor(model => model.PaymentMethod)*@
        @*   @Html.DropDownListFor(model => model.SelectedItemId, ViewBag.PaymentMethods as SelectList)*@
        @*     @Html.DropDownListFor(model => model.SelectedItemId,   new SelectList(ViewBag.ItemsRep, "PaymentMethodId", "Name", 1, "2djdfgbdfjgb"))*@
       @* @Html.DropDownListFor(n => n.SelectedOrderId, Model.OrderTemplates, "Please select an order template")*@
    @*</div>*@

    <h3>Опции</h3>

    @*<h3>Адрес доставки</h3>
        <div class="form-group">
            <label>Первый адрес:</label>
            @Html.TextBoxFor(x => x.Line1, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label>Второй адрес:</label>
            @Html.TextBoxFor(x => x.Line2, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label>Третий адрес:</label>
            @Html.TextBoxFor(x => x.Line3, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label>Город:</label>
            @Html.TextBoxFor(x => x.City, new { @class = "form-control" })
        </div>
        <div class="form-group">
            <label>Страна:</label>
            @Html.TextBoxFor(x => x.Country, new { @class = "form-control" })
        </div>

        <h3>Опции</h3>*@
    <div class="checkbox">
        <label>
            @Html.EditorFor(x => x.GiftWrap)
            Использовать подарочную упаковку?
        </label>
    </div>

    <div class="text-center">
        <input class="btn btn-primary" type="submit" value="Process order" />
        @*  @Html.ActionLink("Order", "Checkout", new { cart = ViewBag.Cart, shippingDetails = }, new { @class = "btn btn-primary" })*@
    </div>
}